{
	"name": "seed-rest-api",
	"version": "0.1.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"deploy:dry-run": "wrangler deploy --dry-run",
		"dev": "wrangler dev",
		"test": "node tests/test.mjs"
	},
	"devDependencies": {
		"wrangler": "^4.135.0"
	}
}
